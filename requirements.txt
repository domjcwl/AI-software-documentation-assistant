# AI Software Documentation Assistant
# Pinned versions verified via `pip install --dry-run --report` on this project's
# venv (Python 3.14.6, win_amd64) on 2026-08-01 — the full set below resolves
# together with no dependency conflicts. See planning/decisions.md ADR-011.
#
# Reinstall: pip install -r requirements.txt

# --- Backend framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
python-dotenv==1.2.2

# --- Vector store ---
chromadb==1.5.9

# --- Agent framework / LLM ---
langgraph==1.2.10
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
openai==2.52.0
tenacity==9.1.4

# --- Frontend ---
streamlit==1.60.0

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.23.1
